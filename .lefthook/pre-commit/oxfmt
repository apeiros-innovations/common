#!/usr/bin/env bash

set -euo pipefail

# shellcheck source=../lib/common.sh
source "$(dirname -- "$0")/../lib/common.sh"

common_root="$(common_remote_root)"

if has_oxfmt_root_config; then
  exec mise x -- oxfmt "$@"
fi

exec mise x -- \
  oxfmt \
  --config "$common_root/.oxfmtrc.json" \
  "$@"

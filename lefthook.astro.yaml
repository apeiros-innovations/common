---
pre-push:
  jobs:
    - name: astro validation
      group:
        parallel: true
        jobs:
          - name: astro check
            run: pnpm astro check
            tags:
              - validation
              - astro
              - typescript

          - name: astro build
            run: pnpm astro build
            tags:
              - validation
              - astro
